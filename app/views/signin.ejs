<!DOCTYPE html>
<html>
   <head>
      <title>
         <%=title %>
      </title>
      <link rel="stylesheet" href="../../styles/bootstrap/bootstrap.css" crossorigin="anonymous">
      <link rel="stylesheet" href="../../styles/css/main.css" crossorigin="anonymous">
      <style type="text/css">

      </style>
   </head>
   <body>
    <div id="login">
           <% for(var i in messages) { %>
             <div class="flash"><%= messages[i] %></div>
           <% } %>
           <form class="login" action="/CntrlPanel/signin" method="post">
            <div class="login-screen">

              <div class="form-group centered alert alert-danger">
                <img src="../../images/stop.jpeg" width="40">
                <p class="small">You are not authorized to view this page, signin as admin</p>
              </div>

              <div class="form-group">
                <input class="form-control" type="text" name="username" placeholder="User" />
              </div>

              <div class="form-group">
                <input class="form-control" type="password" name="password" placeholder="Password"/>
              </div> 
            <div>

               <input class="btn btn-default" type="submit" value="Sign In" />
             </div>
           </div>
         </form>
    </div>
</body>
</html>
