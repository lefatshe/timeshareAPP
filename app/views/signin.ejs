<!DOCTYPE html>
<html>
   <head>
      <title>
         <%=title %>
      </title>
      <link rel="stylesheet" href="../../styles/bootstrap/bootstrap.css" crossorigin="anonymous">
      <link rel="stylesheet" href="../../styles/css/main.css" crossorigin="anonymous">
      <style type="text/css">

      </style>
   </head>
   <body>
      <% for(var i in messages) { %>
      <div class="flash"><%= messages[i] %></div>
      <% } %>
      <div class="login">
         <div class="login-screen">
            <div class="app-title">
               <!-- <h4><img src="../../images/cntrl-logo.svg"></h4> -->
            </div>
            <div class="login-form">
               <form action="/CntrlPanel/signin" method="post">
                  <div class="form-group">
                     <input type="text" name="username" placeholder="User"/>
                  </div>
                  <div class="form-group">
                     <input type="password" name="password" placeholder="Password" />
                  </div>
                  <div class="form-group">
                     <p>
                        <input type="submit" value="Sign In" class="btn"/>
                     </p>
                  </div>
               </form>
            </div>
         </div>
      </div>
   </body>
</html>